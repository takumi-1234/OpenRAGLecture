# OpenRAGLecture Environment Variables Example
#
# How to use:
# 1. Copy this file to a new file named ".env" in the same directory.
#    cp .env.example .env
# 2. Fill in the actual values for each variable in the ".env" file.
# 3. The ".env" file is ignored by git and should NEVER be committed.

# -----------------------------------------------------------------------------
# General Application Settings
# -----------------------------------------------------------------------------
# Gin's running mode. Options: debug, test, release
# This affects logging format and performance.
SERVER_MODE=debug


# -----------------------------------------------------------------------------
# Database (MySQL) Settings
# -----------------------------------------------------------------------------
# These values are used by both the `db` service in docker-compose.yml and
# the Go application when connecting to the database.
MYSQL_DATABASE=open_rag_lecture
MYSQL_USER=user
MYSQL_PASSWORD=password
MYSQL_ROOT_PASSWORD=rootpassword


# -----------------------------------------------------------------------------
# External Services API Keys (Secrets)
# -----------------------------------------------------------------------------
# IMPORTANT: These are sensitive credentials. Keep them secure.

# Google AI Studio (Gemini) API Key
# Required for Embedding and LLM generation.
# Get your key from: https://ai.google.dev/
GOOGLE_API_KEY="your-google-api-key-here"

# Google Cloud (Vertex AI) Settings
# Required when using Vertex AI for embeddings or LLMs.
# Your Google Cloud Project ID.
GOOGLE_PROJECT_ID="your-gcp-project-id-here"
# The location (region) of your Vertex AI resources.
GOOGLE_LOCATION=asia-northeast1


# -----------------------------------------------------------------------------
# JWT Authentication Settings
# -----------------------------------------------------------------------------
# This secret key is used to sign and verify JWT tokens.
# Use a long, random, and unpredictable string for production.
# You can generate one with: openssl rand -base64 32
JWT_SECRET_KEY="a-very-long-and-secure-secret-key-for-jwt-changeme"


# -----------------------------------------------------------------------------
# File Storage (Amazon S3) Settings (Optional)
# -----------------------------------------------------------------------------
# These are only needed if you set `storage.type` to "s3" in your config file.
# For local debug with `storage.type: "local"`, these can be left blank.
#
# S3_BUCKET=your-s3-bucket-name
# S3_REGION=ap-northeast-1
# S3_ACCESS_KEY=your-aws-access-key-id
# S3_SECRET_KEY=your-aws-secret-access-key
# S3_ENDPOINT= # Optional: for S3-compatible storage like MinIO